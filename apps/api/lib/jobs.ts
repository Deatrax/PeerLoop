import { Engine, scoreAudience } from '@peerloop/core';
import { sql } from 'drizzle-orm';
import { database } from '../db/client';
import { transact } from './store';
import { dispatchNotifications } from './dispatch';
export async function runJob(job:string){const now=Date.now();const result=await transact(async state=>{const e=new Engine(state,{user_id:'system',now});if(job==='escalation-tick'){const swept=e.sweep(undefined,200);return {swept,...await dispatchNotifications(state,now)};}if(job==='digest-builder')return dispatchNotifications(state,now,true);if(job==='knowledge-staleness'){let stale=0;for(const k of state.knowledge_items)if(k.status!=='RETIRED'&&Date.parse(k.expires_at)<=now&&k.status!=='STALE'){k.previously_verified=k.status==='VERIFIED';k.status='STALE';stale++;}return {stale};}if(job==='score-recompute'){for(const m of state.space_memberships){const recipients=state.request_recipients.filter(r=>r.user_id===m.user_id&&state.requests.some(q=>q.id===r.request_id&&q.space_id===m.space_id));m.notified_count_7d=recipients.filter(r=>now-Date.parse(r.notified_at)<7*86400000).length;m.notified_count_30d=recipients.filter(r=>now-Date.parse(r.notified_at)<30*86400000).length;m.notifications_sent_today=state.notifications.filter(n=>n.user_id===m.user_id&&n.space_id===m.space_id&&n.delivered_channel==='PUSH'&&n.created_at.startsWith(new Date(now).toISOString().slice(0,10))).length;m.accepted_answers_30d=state.messages.filter(a=>a.author_id===m.user_id&&a.is_accepted&&now-Date.parse(a.created_at)<30*86400000&&state.requests.some(r=>r.id===a.request_id&&r.space_id===m.space_id)).length;m.verified_helper=m.accepted_answers_30d>=5;const author=state.space_memberships.find(a=>a.space_id===m.space_id&&a.user_id!==m.user_id);m.helper_score=author?scoreAudience([m,author],author,[],now).shortlist.find(s=>s.member.id===m.id)?.terms.total??0:0;}return {members:state.space_memberships.length};}throw new Error('Unknown job');},{cron:job==='escalation-tick'});await database().execute(sql`insert into job_runs(id,finished_at,result) values(${job},now(),${JSON.stringify(result)}::jsonb) on conflict(id) do update set finished_at=now(),result=excluded.result`);return result;}
